# Changelog

이 프로젝트의 버전별 주요 변경 사항을 기록합니다. [Keep a Changelog](https://keepachangelog.com/) 형식을 따르고, [시맨틱 버저닝](https://semver.org/lang/ko/)을 사용합니다.

## [Unreleased]

### Added
- 그래프의 X/Y축을 더블클릭하면 그 축의 Min/Max/Label을 바로 편집할 수 있는 다이얼로그 추가. Y축 라벨을 바꾸면 범례에 표시되는 이름도 함께 바뀜
- 파일 목록에서 드래그로 파일 순서를 바꿀 수 있음(결합 결과도 새 순서로 재정렬)
- 파일 목록에서 파일을 더블클릭하면 그 파일이 결합된 데이터에서 차지하는 구간을 그래프에 노란 배경으로 표시. 빈 여백을 더블클릭하면 해제

### Changed
- Figure options(Customize) 다이얼로그에서 축(Axes) 편집 탭 제거 — 축 편집은 그래프에서 해당 축을 더블클릭하는 방식으로 대체
- Customize에서 커브(선) 색을 바꾸면 그 축의 라벨/눈금 색과 범례 색도 함께 갱신되도록 변경
- 툴바의 Pan 버튼을 없애고, 그래프 위에서 왼쪽 버튼 드래그(이동)/오른쪽 버튼 드래그(확대·축소)가 버튼을 켜지 않아도 항상 되도록 변경
- 왼쪽 파일 패널을 어두운 테마에서 밝은 테마로 변경
- 그래프 영역(배경/격자/커브 색상/범례 상자/툴바)을 DAQsystem(INCA·CANalyzer 스타일) 그래프와 같은 톤으로 변경
- 축(X/Y) 편집 다이얼로그의 Min/Max 값을 소수점 4자리로 반올림해서 표시

### Fixed
- 그래프에 Y축이 2개 이상(멀티 Y축)일 때 범례를 마우스로 드래그해도 움직이지 않던 문제 수정(Y축 1개일 때만 우연히 동작했음). 컬럼 체크/해제로 그래프를 다시 그려도 옮겨둔 범례 위치가 유지되도록 함께 수정

## [v0.2.0] - 2026-07-23

### Added
- 그래프 확대/축소/이동 기능: 툴바로 부분 확대(드래그 사각형), 이동, 원래대로, 그래프 이미지 저장, 마우스 휠로 커서 위치 중심 확대/축소
- CSV/Excel 파일을 MDF(.mf4)로 변환해서 저장하는 "MDF로 변환" 버튼 추가. 이후 원본 대신 변환된 mdf 파일을 불러오면 특히 Excel 기준으로 훨씬 빠르게 열림
- Y축으로 체크한 컬럼마다 별도의 축을 만들어 표시(멀티 Y축) — 스케일이 서로 다른 신호도 한 그래프에서 눌리지 않고 함께 볼 수 있음
- 그래프 범례를 마우스로 드래그해서 원하는 위치로 옮길 수 있음

### Fixed
- 대용량 파일(수십만~수백만 행)을 결합한 뒤 그래프를 그리면 무한정 멈춰 있던 문제 수정. X축이 타임스탬프 같은 텍스트 컬럼일 때 matplotlib이 이를 범주형으로 처리해 값마다 변환하느라 느려지던 것과, 범례 위치를 자동으로 찾느라 걸리던 시간이 원인이었음
- 그래프를 마우스 휠로 확대한 뒤 "원래대로" 버튼을 눌러도 처음 상태로 돌아오지 않던 문제 수정

### Changed
- Y축 목록에서 Status, Save Period처럼 숫자로 변환할 수 없는 컬럼은 아예 후보에서 제외
- 파일을 결합해도 그래프가 곧바로 나타나지 않고, Y축 체크박스를 직접 체크한 컬럼만 그래프에 표시되도록 변경
- 파일 목록에 전체 경로 대신 파일명만 표시
- Figure options(Customize)에서 X/Y축 Scale(linear/log/symlog/logit) 설정 기능 제거
- 어두운 사이드바 + 밝은 콘텐츠 영역의 모던 SaaS 대시보드 스타일로 UI 리디자인
- 파일 목록에서 복수 선택(Ctrl/Shift+클릭) 가능하도록 변경. "결합하기"와 "MDF로 변환"은 이제 전체 파일이 아니라 선택한 파일에만 적용됨
- 배포용 exe 빌드 방식을 onefile에서 onedir로 변경(속도, USB 실행 안정성, 실행 차단 이슈 개선) — 배포 시 exe 하나가 아니라 `DataViewer` 폴더 전체를 옮겨야 함

## [v0.1.0] - 2026-07-21

### Added
- CSV(.csv), Excel(.xlsx/.xls) 파일을 드래그 앤 드롭 또는 파일 선택 버튼으로 불러오기
- 같은 열 구조를 가진 여러 파일을 행 단위로 결합, 열 구조가 다른 파일은 자동 제외 후 안내
- 결합된 데이터를 표(QTableView)로 확인
- X축/Y축 선택 후 선 그래프로 시각화
- Y축 다중 선택 및 체크박스로 그래프에 즉시 표시/숨김 토글, X축으로 선택된 열은 Y축 목록에서 자동 제외
- 한글 라벨이 깨지지 않도록 matplotlib 폰트 설정
- PyInstaller 기반 단일 실행파일(exe) 빌드, GitHub Releases로 배포
